<h1>Tasks</h1>
<%= link_to'Add a new task', new_path %>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %>
      <%= link_to 'borrar', destroy_path(task), method: :delete %>
    </li>
  <% end %>
</ul>
